<style>
  .container {
    width: 100%;
    display: flex;
    justify-content: center;
    align-items: center;
    flex-direction: column;
  }

  .answers {
    display: flex;
    justify-content: space-around;
  }

  .answers a {
    padding: 10px;
    background-color: #55a5ff;
    color: white;
    border-radius: 6px;
    width: 1rem;
    margin: 0 5px;
    text-align: center;
    text-decoration: none;
  }

  .recommendation {
    width: 100%;
    margin: 10px;
    display: flex;
    justify-content: space-around;
  }
</style>

<div class="container">	
  <label>Olá <strong>{{name}}</strong>! Tudo bem?</label>
  <h3>{{title}}</h3>
  <br/>
  <strong>{{description}}</strong>

  <div class="recommendation">
    <span>Pouco provável</span>
    <span>Muito provável</span>
  </div>
  
  <div class="answers" >
    <a href="{{url}}/1?u={{user_id}}">1</a>
    <a href="{{url}}/2?u={{user_id}}">2</a>
    <a href="{{url}}/3?u={{user_id}}">3</a>
    <a href="{{url}}/4?u={{user_id}}">4</a>
    <a href="{{url}}/5?u={{user_id}}">5</a>
    <a href="{{url}}/6?u={{user_id}}">6</a>
    <a href="{{url}}/7?u={{user_id}}">7</a>
    <a href="{{url}}/8?u={{user_id}}">8</a>
    <a href="{{url}}/9?u={{user_id}}">9</a>
    <a href="{{url}}/10?u={{user_id}}">10</a>
  </div>

  <br/>
  <br/>

  <strong>Sua opinião é muito importante para nós!</strong>
  <h4>Equipe | NLW</h4>
</div> NLW